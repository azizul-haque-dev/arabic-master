// TODO: replace with your actual toast/notification library,
// e.g. react-native-toast-message, sonner-native, or burnt
export function showErrorToast(message: string) {
  console.warn("[toast]", message);
}
